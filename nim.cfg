# Output directory configuration
# Separate compiled binaries from source files
--outdir:bin

# macOS Metal framework compilation
# Metal.h is Objective-C, so we need to compile as Objective-C
@if macosx:
  --passC:"-x objective-c"
@end
